# Base URL of the deployed API. Leave unset locally: Vite proxies /api to
# http://localhost:4000. In Vercel set it to the Render service, including
# the /api/v1 suffix.
VITE_API_URL=https://lexpatent-api.onrender.com/api/v1
